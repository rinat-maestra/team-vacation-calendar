# Team Vacation Calendar

Live page: `calendar.html` (see `index.html`, which redirects to it).

This is regenerated daily from the "Team Vacation Tracker" Notion database.
See [`calendar-automation-spec.md`](./calendar-automation-spec.md) for the full
regeneration spec — give that file to a Claude Code session to refresh the data.
